# 安全政策

请勿通过公开 Issue 披露未修复漏洞、真实凭证或利用细节。请通过 GitHub 仓库所有者 `daquan088` 的私下联系方式报告，并提供受影响版本、复现步骤和影响范围。

部署者必须使用自己的 API Key、云账号和数据库，不得依赖作者的线上额度。密钥只能放在服务端 Secret 或环境变量中；公开服务应配置额度限制、IP 限流、请求体限制和同源校验，并向用户说明照片会发送给哪一家模型服务商。

若发现真实 Key 泄露，应立即撤销和轮换；仅删除 Git 历史不能保证安全。
